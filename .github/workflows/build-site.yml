name: Build site

on:
  workflow_dispatch:
  schedule:
    # 7am UTC (12am PST) every day
    - cron: '0 7 * * *'

jobs:
  compute:
    runs-on: ubuntu-latest

    steps:
      - name: Build
        run: |
          curl -X POST -d '{}' ${{ secrets.BUILD_SITE_URL }}
